[package]
publish = false
name = "sorng-commands-infra"
version.workspace = true
edition = "2021"
description = "Compatibility facade for bounded infrastructure command registrars"

[dependencies]
sorng-commands-bmc = { path = "../sorng-commands-bmc" }
sorng-commands-virtualization = { path = "../sorng-commands-virtualization" }
sorng-commands-proxmox = { path = "../sorng-commands-proxmox" }
sorng-commands-nas = { path = "../sorng-commands-nas" }
sorng-commands-remote = { path = "../sorng-commands-remote" }
tauri = { workspace = true }
