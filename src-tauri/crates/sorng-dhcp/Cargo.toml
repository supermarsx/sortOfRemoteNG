[package]
name = "sorng-dhcp"
version = "0.1.0"
edition = "2021"
rust-version = "1.77.2"
description = "DHCP server management for SortOfRemoteNG — ISC dhcpd, dnsmasq, Kea DHCP, leases, subnets, reservations."
publish = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
log = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }

[dev-dependencies]
tokio-test = { workspace = true }
