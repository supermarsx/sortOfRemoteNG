[package]
publish = false
name = "sorng-dashboard"
version.workspace = true
edition = "2021"
description = "Connection health dashboard engine for SortOfRemote NG — background threaded health monitoring with widgets, status aggregation, sparkline data, alert feeds, quick stats computation, and low-overhead periodic polling"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
log = { workspace = true }
