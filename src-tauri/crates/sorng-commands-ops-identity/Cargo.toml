[package]
publish = false
name = "sorng-commands-ops-identity"
version.workspace = true
edition = "2021"
description = "Bounded ops-identity native command registrar"

[dependencies]
sorng-freeipa = { path = "../sorng-freeipa" }
sorng-hashicorp-vault = { path = "../sorng-hashicorp-vault" }
sorng-mac = { path = "../sorng-mac" }
sorng-pam = { path = "../sorng-pam" }
tauri = { workspace = true }
serde_json = { workspace = true }
