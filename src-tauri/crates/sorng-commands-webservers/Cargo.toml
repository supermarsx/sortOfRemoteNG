[package]
name = "sorng-commands-webservers"
version = "0.1.0"
edition = "2021"
description = "Webserver/container command handlers — split from services to reduce coherence checking time"

[dependencies]
sorng-lxd = { path = "../sorng-lxd" }
sorng-vmware-desktop = { path = "../sorng-vmware-desktop" }
sorng-nginx = { path = "../sorng-nginx" }
sorng-traefik = { path = "../sorng-traefik" }
sorng-haproxy = { path = "../sorng-haproxy" }
sorng-apache = { path = "../sorng-apache" }
sorng-caddy = { path = "../sorng-caddy" }
sorng-nginx-proxy-mgr = { path = "../sorng-nginx-proxy-mgr" }
sorng-ddns = { path = "../sorng-ddns" }
tauri = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
log = { workspace = true }
chrono = { workspace = true }
uuid = { workspace = true }
base64 = { workspace = true }
