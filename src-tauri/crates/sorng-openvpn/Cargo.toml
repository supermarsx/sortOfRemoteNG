[package]
name = "sorng-openvpn"
version = "0.1.0"
edition = "2021"
description = "Specialized OpenVPN connection management – process lifecycle, management interface, tunnel monitoring, routing, DNS, auth, config generation, and logging"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["full"] }
tauri = { version = "2.9.5", features = ["devtools"] }
uuid = { version = "1.0", features = ["v4"] }
chrono = { version = "0.4", features = ["serde"] }
log = "0.4"
regex = "1.10"
base64 = "0.21"
sha2 = "0.10"
hex = "0.4"
async-trait = "0.1"
