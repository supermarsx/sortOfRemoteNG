[package]
name = "sorng-commands-platform"
version = "0.1.0"
edition = "2021"
description = "Platform command handlers — split from app crate to reduce coherence checking time"

[dependencies]
sorng-app-domains = { path = "../sorng-app-domains", default-features = false, features = ["platform"] }
tauri = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
log = { workspace = true }
chrono = { workspace = true }
uuid = { workspace = true }
base64 = { workspace = true }
