# Blank context lines in vendored unified diffs contain the required marker.
src-tauri/native/ports/**/*.patch -whitespace

# Official vendored bytes have pinned hashes (and upstream mixed line endings).
# Preserve API, license and generated provenance through Git clean/smudge filters.
src-tauri/crates/sorng-protocols/src/vendor/darkreader/* -text whitespace=cr-at-eol
